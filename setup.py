from setuptools import setup, find_packages

setup(
    name="manim_slide",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
    ],
    long_description="SlideScene for manim",
)
